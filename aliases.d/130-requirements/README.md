# 130-requirements

Atajos para sincronizar dependencias usando pip y archivos requirements.

## Funciones
- requirements_install [archivo]: actualiza pip e instala el contenido del archivo indicado (por defecto requirements.txt).
- requirements_generate: ejecuta pip freeze > requirements.txt para capturar el estado actual del entorno.
